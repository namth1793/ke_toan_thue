[build]
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA: redirect tất cả routes về index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
