[build]
  command = "npm install && npx vite build"
  publish = "dist"

[build.environment]
  NODE_VERSION    = "20"
  ENABLE_COREPACK = "0"

# SPA: redirect tất cả routes về index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
